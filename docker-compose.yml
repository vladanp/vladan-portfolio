services:
  hugo:
    image: hugomods/hugo:debian-std-0.165.0
    ports:
      - "1313:1313"
    volumes:
      - .:/src
    command: server --bind 0.0.0.0 --baseURL http://localhost:1313 --disableFastRender --noHTTPCache
